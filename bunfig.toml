# Тесты ОБЯЗАТЕЛЬНО изолируются от реальной data/: preload выставляет TG_DATA_DIR на
# временную папку ДО загрузки любого тестового модуля (иначе lib/paths.ts закэширует
# реальный путь и тесты запишут в живую data/). См. tests/_env.ts.
[test]
preload = ["./tests/_env.ts"]
